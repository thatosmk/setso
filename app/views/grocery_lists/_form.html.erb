<%= form_with(model: grocery_list, local: true) do |form| %>
  <% if grocery_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grocery_list.errors.count, "error") %> prohibited this grocery_list from being saved:</h2>

      <ul>
        <% grocery_list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :total_amount %>
    <%= form.number_field :total_amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
